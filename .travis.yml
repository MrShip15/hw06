language: cpp
jobs:
  include:
  - os: osx
    script:
    - cmake -H. -B_build
    - cmake --build _build
    - cd _build
    - cpack -G DragNDrop
    - cd ..
addons:
  apt:
    packages:
      - cmake
      - cmake-data
      - rpm
